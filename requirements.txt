# 量化交易系统 v7.6 依赖
# 安装: pip install -r requirements.txt

# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# 数据获取
requests>=2.31.0
akshare>=1.12.0

# 可视化与前端
# v8.7 审查修复：代码使用 st.dataframe/st.plotly_chart 的 width="stretch" 与 st.toggle，
# 这些 API 在 1.30 并不存在，下限提到 1.44（实测 1.58/1.59 可用）。
streamlit>=1.44.0
plotly>=5.18.0

# 桌面端套壳（PyWebView - launcher.pyw）
pywebview>=5.0,<7.0

# 系统工具
python-dateutil>=2.8.0
scipy>=1.10.0

# 开发/验证（README「验证」一节依赖；v8.7 补：此前 .venv 缺 pytest 导致 README 命令跑不起来）
pytest>=8.0
