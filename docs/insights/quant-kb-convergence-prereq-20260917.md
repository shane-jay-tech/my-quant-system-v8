# quant KB 双读收敛前置条件核验（917-28，零落码）

**结论（单一）：技术上**可进入删分支**——三前置两项满足、一项结构性保证**；删分支本身属行为变更，正式落地仍须日间拍板（n916d-19 §四 口径不变）。零改码（相关 py 文件 git diff --stat 为空）。**

## 一、三前置逐条核验（命令与输出同段）

| # | 前置 | 命令与输出 | 判定 |
|---|---|---|---|
| 1 | KB_FILE 存在且含 [待验证] 条目 | `ls -l docs/knowledge/quant-kb.md` → 476B @ 09-16 01:07 ✓ | ✅ 满足 |
| 2 | CLAUDE.md 指针行已指新源 | `sed -n '14p' CLAUDE.md` → 「机器写入已迁移至 docs/knowledge/quant-kb.md（c453，过渡期双读）」 ✓ | ✅ 满足（「过渡期双读」字样随删分支一并清） |
| 3 | 观察窗 stderr 零「回退读取 CLAUDE.md 旧节」 | `rg -c '回退读取 CLAUDE.md 旧节' logs/` → **0 命中**；且仓内无运行 stderr 日志留存 | ✅ 无告警命中；**「回退从未实际触发」无运行日志佐证＝待观察项**（结构性上 KB_FILE 恒在 ⇒ 不会走回退） |

补强论证：前置 3 的结构性保证——KB_FILE 自 9/16 01:07 起恒在盘，`_read_kb_content` 回退分支仅在 KB_FILE 缺失时触达；观察窗（9/16→今）内 KB_FILE 从未缺失 ⇒ 回退不可能被触发。

## 二、「可否进入删分支」结论

- **可进入删分支（技术前置齐备）**：删 evolve_strategy.py / integrate_knowledge.py 两处回退分支＋CLAUDE_MD 常量＋过期文案（n916d-19 §二步骤），单 commit 可回滚。
- **执行仍待日间拍板**：删分支＝行为变更（KB_FILE 意外缺失时由「静默回退旧源」变为「读空」）——建议拍板时同步加「KB_FILE 缺失即启动告警」哨兵。

## 三、验收情况

- ✅ 三前置各有命令与输出（§一表格）；✅ 命中数给复算命令（rg 0 命中）；✅ 结论单一附依据；✅ 相关 py 文件 git diff --stat 为空（零改码）；不 push。

## 遗留问题

待日间：①删分支拍板（含哨兵建议）；②拍板通过后按 n916d-19 §二单 commit 落地＋同夜观察 evolve 全流程。
