# 测试覆盖率审查与补测计划（锁定版）

## 目标

- 量化当前测试的行/分支覆盖率，并找出高风险未覆盖路径。
- 优先补齐交易决策、风控、回测、配置与流水线的正常、边界、异常测试。
- 不修改生产逻辑，不访问真实网络，不读写真实交易数据。

## 实施步骤

1. 运行现有 183 项测试并记录覆盖率基线。
2. 将生产模块按资金风险与覆盖缺口排序，人工核对关键分支。
3. 通过项目多模型入口让 GPT 提出补测方案、DeepSeek 独立复核。
4. 仅在 `tests/` 和测试配置/归档文档中新增内容。
5. 每个新增 Python 测试文件写完立即单独执行；失败时最多修正 5 次。
6. 运行全量测试和分支覆盖率，检查完整差异与工作区污染。

## 边界

- 不改变 buy/sell、止损止盈、仓位、回测计算或其他生产行为。
- 测试必须使用 `tmp_path`、monkeypatch 或内存对象隔离文件与外部调用。
- 不以“覆盖行数”为目的执行模块入口或真实流水线。
- 不提交、不推送最终测试改动；由用户检查后决定是否提交。

## 验收命令

```powershell
python -m pytest -q
python -m coverage run --branch --source=. -m pytest -q
python -m coverage report
```

## 验收标准

- 全量测试通过。
- 新测试覆盖至少 3 个高风险模块的关键缺失分支。
- 总覆盖率和关键模块分支覆盖率均较基线提升。
- 测试结束后生产数据文件无新增改动。
