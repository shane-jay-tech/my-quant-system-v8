# 量化交易系统 - 项目规范

> **当前版本**：v8.6 · 发布日期 2026-05-23
> **版本号单一事实源**：`core/config.py:SYSTEM_VERSION` / `data/system_config.json:system.version`
> 所有报告生成器、自检模块、监控面板均从此源拉取版本号；如需升级版本，仅改两处。

---

## ⚡ 协作方法（v2 三模型）— 规则见根配置 D:\code\AGENTS.md

本项目的多模型协作规则统一由根配置决定（总指挥 DeepSeek V4 Pro + 快枪手 V4 Flash + 第二只手 ChatGPT 5.6，走本机 Codex CLI 直连），本文件不再维护协作配置副本，只保留量化项目专属的强制触发清单。

### 量化项目特殊放行（多模型必触发 → 对应根配置「双实现触发 / 升深档」）

以下场景**强制**走多模型协作，即使看起来很小：
- 任何**改 buy/sell 决策逻辑**的代码（直接关系真金白银）
- **风控规则、止损止盈、仓位计算**
- **新增数据源 / 切换数据源**（错一个字段全盘崩）
- **回测引擎核心代码**改动
- **策略参数调优**前的方案选择

### 模型选择

- 批量分类 / 格式化 / 日志分析 / 报告排版 → Flash；资金与风控相关结论永不交给 Flash 或 GPT 单独定夺（根配置质量护栏）。

### 输出风格

- 与根配置一致：全程中文大白话；中间产出幕后，归档到 D:\code\my-quant-system-v8\docs\decisions\YYYY-MM-DD-<slug>.md；用户只看到结论 + 改了什么 + 风险 + 归档路径。

### 与下方「量化项目规范」的关系

下面的所有量化规则（反反爬虫、Debug 自动纠错、SOP、版本约束、API 等）**全部继续生效**。协作是**做事的方法**，量化规则是**做事的内容标准**——两者叠加，不互斥。例如：写一个新爬虫时，既要遵守「反反爬虫 Skill」的全部规范（内容），也要按根配置的协作流程（方法）。

---
## 角色

你是我的专属量化交易系统开发工程师与研究员。

## 反反爬虫 Skill（写爬虫时强制启用）

- 使用 `requests` 库，带 `Session` 保持连接
- 使用 `fake_useragent` 生成随机 User-Agent，如需更高强度则轮换 UA 列表
- 请求间隔：`time.sleep(random.uniform(1, 3))`
- 自动重试机制：异常/非 200 状态码时最多重试 3 次，指数退避
- 优先尝试通过 API 接口抓取；如必要再解析 HTML，但必须绕过常见字体反爬（如提取 CSS 偏移映射）
- 如果返回 403/检测到验证码，自动切换备选数据源（如从东方财富切换到同花顺）
- 所有爬虫代码必须 include try-except 和 print 详细日志
- 爬虫知识源：`books/反反爬实战笔记.md`（如果存在）

## Debug 与自动纠错 Skill（全局启用）

- 编写任何 .py/.sh 文件后，必须立即执行一次
- 如果终端输出包含 Traceback/Error：
  1. 分析错误信息
  2. 修改代码
  3. 再次执行
  4. 重复此过程，最多尝试 5 次，直到运行成功
- 所有脚本必须返回清晰的成功/失败状态码

## 量化工作流 SOP

当我说"执行量化流程"或提到"今日选股"时，严格按下面顺序执行：

1. 检查 `data/` 目录，判断是否已存在当日数据
2. 如数据缺失或过期 → 运行爬虫获取最新数据
3. 清洗数据（列名标准化、缺失值处理、类型转换）
4. 执行选股策略
5. 将结果输出为 Markdown 表格到 `results/pick_YYYYMMDD.md`
6. 生成一份 100 字以内的文字摘要
7. 如有问题自动修复，保证最后一定有产出

## 模型选择

- 简单格式化或日志分析等次要任务使用轻量模式，无需手动切换

## 记忆机制

- 每次任务完成后，在 `memory.md` 末尾追加记录
- 下次对话开始前，先读取 `memory.md` 获取历史上下文

# 量化策略知识库

### 反反爬知识库：反反爬实战笔记
> 来源：`books/反反爬实战笔记.md` | 章数：10

**核心章节**：
- 一、请求头伪装（User-Agent、Referer 轮换）
- 二、延时与随机间隔
- 三、自动重试与指数退避
- 四、IP 代理池
- 五、字体反爬识别与绕过

**使用方法**：写爬虫时启用「反反爬虫 Skill」，所有反爬模式自动应用。

---

### 如何减少 v4 策略 17.9% 的死叉自然出场率同时保留利润
> 来源：`reports/research_20260513_如何减少_v4_策略_17_9_的死叉自然出场率同时保留利润.md` | 整合日期：20260513

**核心发现**：
- 死叉出场是一把双刃剑
- 均线参数敏感性
- 出场信号多元化

**可落地建议**：
- 建议1：引入「三重出场」机制  [[待验证]
- 建议4：加入「连涨/连跌天数」过滤  [[待验证]

---

### 量化策略研究报告
> 来源：`reports/daily_insight_20260513.md` | 整合日期：20260514

**核心发现**：
- 出场信号多元化
- v5 基准数据
- 因子拥挤风险

**可落地建议**：
- 建议4：连涨/连跌天数过滤

---

### 外部研究：arXiv 量化金融论文 [来源: arXiv]
> 来源：`reports/external_research_20260514.md` | 整合日期：20260514

**论文发现**：
- 研究量化投资领域的相关课题，为策略优化提供理论基础
- 探索机器学习/深度学习方法在股票收益预测中的应用，对比传统因子模型的预测能力
- 研究量化投资领域的相关课题，为策略优化提供理论基础

**可落地因子建议**：
- [待验证] 【风险管理】动态调整仓位以控制最大回撤
- [待验证] 【价值因子】低市盈率/低市净率股票可能提供额外alpha
- [待验证] 【流动性】流动性溢价——低流动性股票可能提供超额收益
- [待验证] 【机器学习】可尝试用ML模型（XGBoost/LSTM）替代线性评分
- [待验证] 【波动率】低波动异象——低波动股票长期收益可能高于高波动

---

### 量化策略研究报告
> 来源：`reports/research_20260513_今日市场特征复盘_v5策略MA_5_30_未来3天胜率预估_大盘择时信号_死叉边.md` | 整合日期：20260514

**核心发现**：
- 死叉出场是一把双刃剑
- 均线参数敏感性
- 出场信号多元化

**可落地建议**：
- 建议1：引入「三重出场」机制
- 建议3：RSI 阈值根据大盘状态动态调整
- 建议4：加入「连涨/连跌天数」过滤

---

### 量化策略研究报告
> 来源：`reports/daily_insight_20260518.md` | 整合日期：20260518

**核心发现**：
- 出场信号多元化
- v5 基准数据
- 因子拥挤风险

**可落地建议**：
- 建议3：RSI 阈值动态调整
- 建议4：连涨/连跌天数过滤

### 量化策略研究报告
> 来源：`reports/daily_insight_20260617.md` | 整合日期：20260617

**核心发现**：
- 出场信号多元化
- v5 基准数据
- 因子拥挤风险

**可落地建议**：
- 建议3：RSI 阈值动态调整
- 建议4：连涨/连跌天数过滤


# 成本优先原则

- 所有数据清洗、格式转换、简单计算（RSI/MA/ATR）必须在本地完成
- LLM仅用于：决策判断、市场研究、策略进化和异常修复
- 每100行代码生成≈2K token，每篇研究报告≈4K token（预估模型）
- 每日成本审计报告：`reports/cost_audit_YYYYMMDD.md`
- 纯本地步骤12个（fetch/strategy/position等），LLM步骤6个（research/evolve等）

# 系统架构（v7.6 起，沿用至 v8.6）

> 下述模块自 v7.6 起加入，v8 全部继承并通过 `core/pipeline.py` DAG 注册表统一调度。

## v7.6 模块清单（沿用）

| 模块 | 文件 | 功能 |
|------|------|------|
| 数据接入层 | `data_loader.py` | AKShare 基本面(ROE/净利润增速/负债率) + 北向资金 + 主力资金 + 市场情绪 |
| 因子分析 | `factor_analysis.py` | 技术因子 IC/IR 计算 + 基于 IR² 的动态权重推荐 |
| 组合风控 | `portfolio_risk.py` | 持仓相关性矩阵 + VaR/CVaR + 回撤硬止损(>10%强制降仓) + 波动率目标 + 换手控制 |
| 盘前预演 | `premarket_sim.py` | 集合竞价情绪评分(0-100) + 开盘仓位建议(空仓/20%/40%/60%/80%) |
| Walk-Forward | `walk_forward.py` | 滚动训练/测试窗口 + 参数网格搜索 + 过拟合比率统计 |
| 蒙特卡洛 | `monte_carlo.py` | 随机滑点/起始日偏移/参数微扰 + 收益分布 + 破产概率估计 |
| 策略竞技 | `strategy_arena.py` | 5套参数并行竞技 + 月度淘汰末位 + 基于优胜者变异进化（**v8.6 默认关**：小资金价值低，需 `evolve_priority.arena_enabled=true` 才启用） |
| 基本面过滤 | `strategy.py` v7.6 | 停牌/退市/新股过滤 + ROE>0/净利润增速>-20% 硬性过滤 |
| 配置缓存 | `core/config.py` v7.6 | 单例缓存 + reload() + get_mtime()，消除全局变量污染 |
| 分档成本 | `enhanced_backtest.py` v7.6 | 按市值分档回测成本(大盘0.15%/中盘0.20%/小盘0.30%) |

## v7.5 新增模块 (2026-05-14)

| 模块 | 文件 | 功能 |
|------|------|------|
| 分钟K线v2 | `fetch_minute_kline.py` | 智能重试+双数据源(东方财富/新浪)+自动降级 |
| 反馈冷启动 | `strategy_feedback.py` v2 | 真实交易不足时自动加载回测数据(442笔) |
| 成本审计 | `cost_tracker.py` | LLM调用成本追踪+每日审计报告+仪表盘看板 |
| 新手保护期 | `newbie_protection.py` | 3阶段保护(观察/模拟/预备)，自动阶段推进 |
| 轻量进化 | `evolve_daily_light.py` | 每日参数微调(RSI±5/MA±2/仓位±5%)+安全锁 |
| 自检自愈 | `_self_check.py` + `auto_heal.py` | 每日管道结束后70项自检→自动修复→日志记录 |
| 首周手册 | `learning/first_week_guide.md` | 新手10天从观察到实盘的完整路径 |
| 出场顾问 | `exit_advisor.py` | 5级出场条件(止损/止盈/到期/死叉/弱势)每日检查所有持仓 |
| 选股追踪 | `pick_tracker.py` | 追踪每日选股次日表现，积累准确率数据 |
| 交易分析器 | `trade_analyzer.py` | 10种智能提醒+持仓分析+行为评估 |
| 完整调仓计划 | `send_to_bark.py` v5 | 卖出→买入资金闭环，自动计算回收资金→分配买入 |

## 进化器优先级（v8.6 整理）

系统里有三个进化器都在改 RSI/MA/仓位上限这些核心参数。为了避免互相覆盖，明确分工如下：

| 进化器 | 节奏 | 作用范围 | 写入路径 | 默认开关 |
|---|---|---|---|---|
| `evolve_daily_light.py` | 日 | RSI±5 / MA±2 / 单票仓位±5% | `data/evolve_daily_state.json` | **当下生效路径**（始终开） |
| `evolve_strategy.py --auto` | 周（周四） | 预设方向性 A/B 变体 | 改 `enhanced_backtest.py` 源码常量 | 开（结果作为 [待验证] 写回 CLAUDE.md，待人工 confirm） |
| `strategy_arena.py` | 月（周五） | N 套变异组并行竞技 | `data/arena_state.json` | **关**（小资金价值低；需 `evolve_priority.arena_enabled=true`） |

开关位都在 `core/config.py` 的 `evolve_priority.*`，也可以在 `data/system_config.json` 里覆盖。

## v7.5 行为闭环强化 (2026-05-16)

三个细粒度升级，增强"系统训练用户"能力：

### 1. 完整调仓指令 (`send_to_bark.py` v5)
- 读取出场建议 + 买入清单，生成资金闭环调仓计划
- 卖出回收资金按评分从高到低分配给1-2只买入股票
- 资金不足的买入标记为"可选择性执行"
- 格式：`5卖→3买`，含金额计算 + 操作顺序建议

### 2. 表现驱动阶段切换 (`newbie_protection.py` v2)
- 从纯时间驱动升级为4指标综合评分：学习主动度、交易录入度、纪律表现、自评意愿
- 每项25分，总分≥50分建议升级
- 连续3天建议未响应→自动推进（被动升级）
- 时间保底：第15天强制推进
- 仪表盘"学习中心"新增阶段状态面板+进度条+确认按钮
- 心理助手区分主动/被动升级鼓励语

### 3. 轻交互反馈闭环 (`trade_analyzer.py` v2)
- 交易备注分析：恐惧模式检测、跟单正面强化、空备注提醒、误操作检测
- 交易心情记录：仪表盘"我的交易"页底部心情下拉框→`data/trading_mood.csv`
- 周末心情回顾：心理日记自动汇总本周情绪，针对性推送鼓励语
- 所有交互完全可选，不阻塞原有流程

## 日终流水线（v8 DAG 注册表驱动）

> v7.6 是硬编码 24 步 .bat；**v8 已迁移**到 `core/pipeline.py:PIPELINE_STEPS` 注册表，按 `SYSTEM_TIER` 与 `schedule` 自动过滤步骤。如需改流水线，改注册表即可，**不要再回去改 .bat**。
> v7.6 原步骤序列保留在 `core/pipeline.py` 注释，便于对照。

## 新手保护期三阶段 (v2: 表现驱动)

| 阶段 | 天数 | 升级条件 | 推送标注 |
|------|------|----------|----------|
| 观察期 | 第1-5天(最小) | 学习主动度+交易录入度+纪律表现+自评意愿 ≥50分 | 请勿实盘 |
| 模拟期 | 第6-10天(最小) | 同上，4项指标综合评分，≥50分建议升级 | 建议同比例模拟 |
| 实盘预备期 | 第11天起(最小) | 时间保底第15天自动推进 | 系统自动评估 |

- v2升级：表现达标→系统建议升级→用户仪表盘确认(主动)/连续3天未响应自动推进(被动)/第15天强制推进(保底)
- 阶段状态面板：仪表盘"学习中心"页顶部，含4项指标进度条 + "我觉得我准备好了"按钮

# 真实交易数据流（v7.5 起，沿用至 v8.6）

## 为什么需要真实交易数据

系统默认使用模拟交易数据驱动反馈闭环。但模拟和真实世界有差异：
- 模拟交易按收盘价成交，真实交易有滑点
- 模拟交易没有情绪影响，真实交易有恐惧和贪婪
- 模拟交易总能按止损价卖出，真实交易可能跳空

真实交易数据让系统更准确地评估策略表现、优化风控参数。

## 数据优先级

```
real_trades.csv (真实交易) > sim_results/trade_history.csv (模拟交易) > 回测冷启动数据
```

- **real_trades.csv**: 最高优先级。系统认为这是最真实可信的信号。
- **sim_results/trade_history.csv**: 模拟交易记录。次优先级。
- **回测冷启动**: 442笔历史回测数据。仅在前两者数据不足时使用。

## 录入方式

1. **交互式录入**: `python log_real_trade.py` — 按提示输入日期/代码/方向/价格/数量
2. **命令行录入**: `python log_real_trade.py --date 2026-05-14 --code 000001 --direction 买入 --price 11.05 --qty 100 --reason "系统推荐-趋势跟随"`
3. **直接编辑**: 用Excel或文本编辑器编辑 `real_trades.csv`

## 数据格式 (real_trades.csv)

| 字段 | 说明 | 示例 |
|------|------|------|
| 日期 | YYYY-MM-DD | 2026-05-14 |
| 代码 | 6位数字 | 000001 |
| 名称 | 股票简称 | 平安银行 |
| 方向 | 买入/卖出 | 买入 |
| 价格 | 成交均价 | 11.05 |
| 数量 | 股数 | 100 |
| 成交额 | 价格×数量 | 1105.00 |
| 手续费 | 佣金+印花税 | 0.28 |
| 下单依据 | 自由文本 | 系统推荐-趋势跟随 |
| 备注 | 可选 | 示例数据 |

## 手续费计算

- 佣金: 0.025% (买卖双向)
- 印花税: 0.05% (仅卖出)
- log_real_trade.py 自动计算

## 执行质量评估

sim_trade.py 每周自动比对 real_trades.csv 与系统推荐订单：
- **滑点**: (真实成交价 - 系统推荐价) / 系统推荐价
- **纪律偏差**: 真实交易是否在推荐±1%范围内
- **评分**: 优秀(滑点<0.3%) / 良好(滑点<0.5%) / 需改进

# 系统架构 v7（仅作历史参考，v8 不再使用）

## 核心模块（16步完整流水线，0-15）

```
数据层 → 策略层 → 多策略对比 → 执行层 → 反馈闭环 → 研究层 → 学习层 → 心理层 → 新手交付
```

### 数据层
- `fetch_stock_data.py` — 每日全市场A股实时行情（5514只）
- `fetch_history.py` — K线历史增量更新
- `fetch_minute_kline.py` — 60分钟K线 + 日内偏离检测（v7新增）

### 策略层
- `strategy.py` — 趋势跟随：5维因子评分 + 动态参数 + 板块集中度风控
- `multi_strategy.py` — 3策略并行对比：趋势跟随 + 均值回归 + 低波动率 + 加权投票（v7新增）
- `enhanced_backtest.py` — MA(5,30)死叉出场 + 大盘择时 + 基准对比
- `position_sizer.py` — 5档市场状态 → 仓位分配 + ATR止损 + 板块上限 + 日内偏离
- `sector_classifier.py` — 16板块统一分类 + 集中度检测（v7新增）

### 执行层
- `sim_trade.py` — 虚拟账户(10万) + 动态风控参数（可由反馈系统调整）
- `broker_adapter.py` — 订单→券商格式转换（东方财富/同花顺/通用CSV）+ 合规检查（v7新增）
- `send_to_bark.py` — iOS推送，3模式（simple/standard/research）
- `track_performance.py` — 历史选股前向收益追踪

### 反馈闭环（v7新增）
- `strategy_feedback.py` — 历史投票→前瞻收益测量→策略权重更新→风控参数自动调整

### 研究层
- `research_agent.py` — 每日市场复盘研究
- `external_research.py` — arXiv论文检索 + 因子映射

### 学习层
- `integrate_knowledge.py` — 报告→CLAUDE.md知识库
- `evolve_strategy.py --auto` — A/B测试 + 自动进化 + 安全锁

### 心理层
- `psychology_assistant.py` — 每日心态检查 + 亏损安慰 + 学习任务

### 新手交付层（v7新增）
- `newbie_instruction_card.py` — 每日超简指令卡："买入XXX，XXX股，止损XX"
- `.newbie_mode` — 新手模式标志文件，删除即恢复标准模式

### 可视化
- `app.py` — Streamlit仪表盘

## 关键参数

| 参数 | 值 | 说明 |
|------|-----|------|
| MA组合 | (5, 30) 动态 | 根据市场状态自适应调整 |
| RSI区间 | 动态5档 | 强牛25-75 ~ 强熊38-60 |
| 策略数 | 3 | 趋势跟随 + 均值回归 + 低波动率 |
| 投票方式 | 加权共识 | 权重基于20日表现动态调整 |
| 板块风控 | 单板块≤3只, ≤30%仓位 | 16板块分类 |
| 分K线 | 60分钟 | 日内均线偏离检测 |
| 反馈周期 | 每日 | 策略权重 + 风控参数自动更新 |

## 自动化保障

- Windows Task Scheduler:
  - 每个交易日 **09:15** 触发 `morning_pipeline.bat`（盘前预演）
  - 每个交易日 **15:30** 触发 `daily_pipeline.bat`（日终流水线）
- 24步日终流水线（0-24）：数据→数据层(基本面)→选股(含基本面过滤)→多策略→回测(分档成本)→因子分析(周一)→分钟K线→仓位→模拟交易→组合风控→反馈闭环→研究→追踪→内化→心理→新手指令卡→推送→自检自愈→Walk-Forward(周三)→蒙特卡洛(月末)→策略竞技(周五)→每周任务
- 3步盘前流水线：交易日检测→情绪评分→仓位建议推送
- 新手模式：`.newbie_mode` 存在时→生成指令卡 + simple推送
- 安全锁：连续2次进化退化自动暂停
- 组合风控锁：净值回撤>10%或年化波动率>20%时自动拦截新买入订单


## 动态参数规则 [动态参数规则] v7

基于5档市场状态自动调整RSI和MA参数：

| 市场状态 | RSI下限 | RSI上限 | MA快线 | MA慢线 | 逻辑 |
|----------|---------|---------|--------|--------|------|
| 强牛 | 25 | 75 | 5 | 30 | 包容强势股，慢线更长防死叉误触发 |
| 弱牛 | 28 | 72 | 5 | 30 | 略放宽上限，保留趋势空间 |
| 震荡 | 30 | 70 | 5 | 25 | 适中参数，快出慢进 |
| 弱熊 | 35 | 65 | 5 | 15 | 保守选股，快速出场 |
| 强熊 | 38 | 60 | 5 | 10 | 极度保守，极快出场防深套 |

切换开关: `strategy.py` 中 `USE_DYNAMIC_PARAMS = True/False`
验证方法: `evolve_strategy.py` 中 `dynamic_params` 改进类型对比动态vs固定参数

## 小资金实盘注意事项（v7.6→v8.6 持续生效）

> 适用场景：本金 < 3,000 元人民币的 A 股实盘/模拟盘

### 1. A 股券商最低佣金规则

- 通行规则：**每笔买入/卖出佣金不足 5 元按 5 元收取**（万一免五除外，但大多数券商不免）
- 本系统 `sim_trade.py` 已按 `max(成交额 × 0.025%, 5.0)` 建模
- 印花税仅卖出时收取，费率 0.05%，无最低限制

### 2. 小资金被迫集中

- 本金 < 3,000 元时，系统**自动跳过板块集中度限制**（`sector_classifier.apply_sector_cap` 直接 return）
- 原因：A 股最小交易单位 100 股，强制分散会导致大量空仓，资金效率归零
- 单票上限放宽至 100%，允许满仓单只（工程上的被迫选择，非推荐）

### 3. 摩擦成本占比与预期管理

- **每笔完整交易的固定摩擦成本 ≈ 10 元**（买入佣金 5 元 + 卖出佣金 5 元，印花税另计）
- 以 1,200 元本金为例：单次买卖固定成本占本金 **0.8% 以上**
- 若持仓周期短、交易频繁，摩擦成本将快速侵蚀微薄收益
- **坦诚提示**：本金 < 3,000 元时，请把系统当作**学习工具和纪律训练场**，验证策略逻辑和执行力；待本金增大后再追求绝对收益

### 4. 相关代码位置

- 佣金下限模型：`sim_trade.py` — `execute_buy_orders()` / `check_exits()`
- 板块集中度跳过：`sector_classifier.py` — `apply_sector_cap()`
- 小资金模式触发：`position_sizer.py` — `calculate_position_sizes()`（`total_capital <= 3000`）

